# Wedding Xuân & Phượng — v19.5 release-gate hardening

## 13-skill workflow

```text
integrity
→ requirements/live-parity audit
→ research
→ brainstorm
→ architecture/planning
→ UI/UX and accessibility
→ code review
→ debug/root-cause
→ fix/cook
→ static/browser/performance testing
→ optimization loop
→ Fable evidence judge
```

The uploaded integrity evidence records 13/13 skill archives as readable. Their documented methods are applied as the engineering workflow; the archives are not copied into the website runtime.

## Changes

- Add a strict manual `Certify guest release` workflow.
- Fail closed until all venue, Maps, RSVP, deadline and event-state blockers are resolved.
- Add Lighthouse CI budgets for the production `dist` output.
- Add weekly Dependabot updates for GitHub Actions and npm.
- Preserve the existing public deploy workflow; this branch does not invent missing wedding data or mark the invitation guest-ready.

## Lighthouse policy

- Accessibility: hard gate at 0.95.
- Best practices: hard gate at 0.90.
- CLS: hard gate at 0.10.
- Performance score and LCP begin as warnings to establish a stable baseline without introducing flaky releases.
- Total transfer budget begins at 8 MiB as a warning.

## Strict release policy

The manual workflow runs:

```text
release_readiness.py --strict
→ source/contract/assets tests
→ clean dist build
→ browser regression
→ audio/story regression
→ Lighthouse budgets
→ upload evidence
```

It is expected to fail while the current known blockers remain. That failure is intentional and prevents a false guest-ready declaration.

## Remaining external blockers

1. Multi-event RSVP URLs and guest-name entry IDs.
2. Verified bride-home Maps pin and embed.
3. Real Nha Trang venue, address and Maps.
4. Full Sài Gòn address and Maps.
5. RSVP deadlines and event end times.
6. Final guest segmentation data.
7. Physical Android/iPhone and Zalo/Messenger testing.
8. Live RSVP and wishes end-to-end verification.

## Fable verdict

```text
RELEASE GATE HARDENING       ACCEPT
LIGHTHOUSE BASELINE          ACCEPT
DEPENDENCY AUTOMATION        ACCEPT
INVENTING MISSING DATA       REJECT
MORE DECORATIVE ANIMATION    REJECT FOR NOW
GUEST-READY CLAIM            BLOCKED
```
