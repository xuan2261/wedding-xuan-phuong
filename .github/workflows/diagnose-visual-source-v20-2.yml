name: Diagnose visual v20.1 source checks v2

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/diagnose-visual-source-v20-2.yml

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/visual-overlap-v20-1
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - name: Install verification dependencies
        run: python -m pip install --disable-pip-version-check pillow opencv-python-headless playwright
      - name: Run every source check and record failures
        shell: bash
        run: |
          set +e
          mkdir -p reports
          report=reports/VISUAL-SOURCE-DIAGNOSIS-V20.1-V2.log
          : > "$report"
          echo "HEAD=$(git rev-parse HEAD)" >> "$report"
          for file in app.js config.js guest-utils.js event-entry.js; do
            echo "--- node --check $file" >> "$report"
            node --check "$file" >> "$report" 2>&1
            echo "status=$?" >> "$report"
          done
          for test in $(find tests -maxdepth 1 -type f -name '*.py' | sort); do
            echo "--- python $test" >> "$report"
            python "$test" >> "$report" 2>&1
            echo "status=$?" >> "$report"
          done
          cat "$report"
          exit 0
      - name: Commit diagnosis
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f reports/VISUAL-SOURCE-DIAGNOSIS-V20.1-V2.log
          git commit -m "debug(ui): record remaining v20.1 source failures"
          git push origin HEAD:fix/visual-overlap-v20-1
